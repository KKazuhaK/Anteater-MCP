# This project is intentionally dependency-free; node_modules should never appear.
node_modules/
package-lock.json

# Secrets — ANTEATER_API_KEY belongs in the environment, never in the tree.
.env
.env.*
!.env.example

# Editor / OS noise
.DS_Store
.idea/
.vscode/
*.swp

# Local agent + tooling state
.claude/
*.log
